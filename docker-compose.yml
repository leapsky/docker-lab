version: '3'
services:
  app1:
    build: ./app1
    volumes:
      - ./storage:/templates:ro

  app2:
    build: ./app2
    volumes:
      - ./storage:/templates:ro

  nginx:
    build: ./nginx
    ports:
    - "443:443"
    depends_on:
      - app1
      - app2
