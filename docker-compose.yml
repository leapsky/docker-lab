version: '3'
services:
  app1:
    build: ./app1
    volumes:
      # BUG FIX #1: Исправлен путь монтирования для Flask templates
      - ./storage:/app/templates:ro

  app2:
    build: ./app2
    volumes:
      # BUG FIX #1: Исправлен путь монтирования для Flask templates
      - ./storage:/app/templates:ro

  nginx:
    build: ./nginx
    ports:
      # BUG FIX #2: Исправлен маппинг портов для HTTPS
    - "443:443"
    depends_on:
      - app1
      - app2
